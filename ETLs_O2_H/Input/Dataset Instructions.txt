The Dataiku Project has the original 3 datasets included and all the steps to clean it. 

This projects needs to be imported in Dataiku and then select the last dataset in the flow and build it recursively. Then export that dataset and put it in this folder under the "H_2AVisaApplications_stacked_prepared" name (This needs to be a ',' comma separated value file.

Then it should be fine to run the entire job (JOB_H2AVISA)
